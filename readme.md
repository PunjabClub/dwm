```sh
git clone git://git.suckless.org/dwm && cd dwm

git remote rename origin suckless
git remote set-url --push suckless no_push

git remote add origin git@github.com:PunjabClub/dwm.git
git push --set-upstream origin master
```
